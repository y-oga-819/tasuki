/**
 * Hook for review persistence.
 * Currently a no-op — will be reimplemented with gate file persistence in Phase 2.
 */
export function useReviewPersistence() {
  // Phase 2 will implement gate file-based persistence
}
